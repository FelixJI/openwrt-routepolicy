# 安全策略

## 支持范围

当前仅维护最新发布版本在 OpenWrt 25.12 x86_64 上的安全问题。其他 OpenWrt 版本、CPU 架构和本地手工迁移脚本不在发布支持范围内。

## 报告漏洞

请使用仓库的 GitHub Security Advisory 私密报告功能；若该功能尚未启用，请联系仓库维护者并只提供最少必要复现信息。请勿在公开 issue 中提交可利用细节、设备公网地址、配置备份、访问令牌、认证信息或完整带查询参数的订阅 URL。

报告应包含受影响版本、复现条件、影响范围和可行的缓解措施。我们会确认收到报告、评估影响并在修复准备好后协调披露。

## 安全边界

RoutePolicy 仅管理它自己的 nftables 表、策略路由和 SmartDNS 附加片段。它不提供任意命令执行，不接管 LAN/WAN 拓扑，不创建上游 DNS，也不会自动执行旧方案清理。安装包默认禁用；启用前仍需由管理员核对接口和回退路径。
